# `apps/web` — Frontend (Next.js)

Aqui ficará o App Router, login da secretaria, telas de alunos, catálogo e matrícula.

**Ainda não gerado:** rode `create-next-app` nesta pasta quando for o momento, conforme `docs/monorepo-estrutura.md`.

A UI consome a API em `apps/api` (URL base configurada por ambiente, ex. `NEXT_PUBLIC_API_URL`).
